// Task store - placeholder, will be implemented by parallel agent
import { create } from "zustand";

interface TaskStore {
  // placeholder
}

export const useTaskStore = create<TaskStore>(() => ({}));
